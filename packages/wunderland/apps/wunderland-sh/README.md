# Removed Recursive Mirror

This directory previously contained a recursive mirror of the `wunderland-sh` repo.
It has been removed to avoid duplicate sources and nested workspaces.

Canonical source lives at:
- `apps/wunderland-sh` (this repo root)

Do not vendor the repo into `packages/wunderland/apps/wunderland-sh` again.
